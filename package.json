{
  "name": "mlb-generate-images",
  "version": "0.0.1",
  "description": "Program for a AWS lambda that reads S3 images and uses Jimp to make a composition of them",
  "main": "src/handler.js",
  "scripts": {
    "start": "node src/handler.js",
    "debug": "export SLS_DEBUG=* && node --inspect ./node_modules/.bin/serverless offline -s dev",
    "test": "jest"
  },
  "keywords": [
    "aws",
    "lambda",
    "s3",
    "jimp"
  ],
  "author": "Luana Pimentel",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "aws-sdk": "^2.839.0",
    "jimp": "^0.16.1",
    "ramda": "^0.27.1"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "serverless": "^2.42.0",
    "serverless-offline": "^6.8.0"
  }
}
